{
  "name": "shoots",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "description": "shoots — scriptable batch automation for photography workflows (monorepo root)",
  "author": "Stefano Pascazi <stefanopascazi@gmail.com>",
  "license": "PolyForm-Noncommercial-1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "build": "npm run build -w @shoots/core -w @shoots/imaging -w @shoots/inference -w @shoots/cli",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "clean": "node -e \"for (const p of ['core','imaging','inference','cli']) require('fs').rmSync(`packages/${p}/dist`, { recursive: true, force: true })\"",
    "shoots": "node packages/cli/dist/cli.js",
    "build:binary": "bun scripts/build-binary.ts",
    "preversion": "npm run typecheck && npm run build",
    "postversion": "git push origin main && git push origin --tags"
  },
  "devDependencies": {
    "@huggingface/transformers": "^4.2.0",
    "@types/node": "^24.0.0",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3"
  },
  "overrides": {
    "sharp": "^0.35.3",
    "adm-zip": "^0.6.0",
    "onnxruntime-node": "1.27.0",
    "esbuild": "^0.28.1"
  }
}
