node_modules/
dist/
dist-bin/
dist-tools/
dist-models/
*.tsbuildinfo
*.log
.DS_Store
test/
# …but a package's own test suite is source, not scratch. The bare rule above
# guards the root `test/` sandbox (datasets, preview dumps) and matches at every
# level, which silently swallowed packages/cli/test.
!packages/*/test/

# local develop-predictor experiment artifacts (datasets / profiles)
/train_*.json
/train_*.jsonl
/profile*.json
/smke.json

# local develop-predictor experiment artifacts (predictions / emitted sidecars)
/predictions*.json
/out-xmp*/
aistudio/

# screenshot capture staging (scripts/capture-screens.tsx)
/demo/
/webapp/
# Local develop-predictor experiment data (large, machine-specific)
train_v2.jsonl
train_v2_trained.json
train_full.jsonl
train_ext.jsonl
predictions.json
profile-my-style.json
