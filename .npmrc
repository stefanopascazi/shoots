# `npm version <patch|minor|major>` release settings.
# Creates a "vX.Y.Z" tag (matched by the release workflow) and a
# conventional-commit release commit (kept out of the git-cliff changelog).
tag-version-prefix=v
message=chore(release): %s
